//Write a program to read decimal number using command line argument and convert it to binary equivalent.

//•	CODE:
public class t8
{
   public static void main( String args[] ) 
   {
		int n=Integer.parseInt(args[0]);
		int  a;
        String bin = "";
        while(n > 0)
        {
            a = n % 2;
            bin = a + "" + bin;
            n = n / 2;
        }
        System.out.println("Binary value of "+n+" is "+bin);    
   }
}
